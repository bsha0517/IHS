# CLAUDE PROMPT — P3.8 INVENTORY / PROCUREMENT OPERATIONAL UX

## Healthcare Information System

P0, P1, P2, P3.1, P3.2, P3.3, P3.4, P3.5, P3.6 and P3.7 are COMPLETE.

Current verified baseline entering P3.8:

* Local PostgreSQL operational
* Development DB: `his_dev`
* Integration test DB: `his_test`
* Remote Supabase NOT used by integration tests
* 33/33 migrations applied
* Prisma validation clean
* TypeScript clean
* Lint clean
* Production build clean
* 49 integration test files
* 324 integration tests
* 324/324 passing

Completed P3 workflows:

* P3.1 Reception / Appointments
* P3.2 Patient 360
* P3.3 Doctor / Encounter
* P3.4 Nursing / Vitals
* P3.5 Laboratory / Radiology
* P3.6 Pharmacy / Dispensing
* P3.7 Billing / POS / Cashier

Do NOT reopen completed batches unless P3.8 introduces a direct regression.

---

# 1. PURPOSE

P3.8 is:

# INVENTORY / PROCUREMENT OPERATIONAL UX

This is NOT another whole-system audit.

The objective is:

> Make day-to-day stock, batch, expiry, transfer, adjustment, supplier and procurement workflows operationally coherent while preserving the existing inventory ledger, FEFO, purchasing, AP and accounting architecture.

Conceptual operational paths:

## Inventory

Product
→ Stock
→ Batch
→ Stock Ledger
→ Adjustment / Transfer / Consumption

## Procurement

Purchase Request
→ Purchase Order
→ Supplier
→ Goods Receipt
→ Batch / Stock
→ Supplier Invoice / AP

Do NOT rebuild the inventory engine.

Do NOT rebuild Finance.

---

# 2. EXISTING ARCHITECTURAL BASELINE

Earlier phases already implemented/hardened:

* Product catalog
* ProductBatch
* StockLedgerEntry
* FEFO
* expired-batch exclusion
* stock consumption
* POS stock movement
* Pharmacy stock movement
* void stock reversal
* stock adjustments
* batch-aware adjustments
* expired-batch write-off
* stock transfers
* Purchase Requests
* Purchase Orders
* Goods Receipts
* Suppliers
* Supplier Invoices / AP
* reconciliation
* accounting handoff

Preserve these.

P3.8 is primarily operational workflow and UX.

---

# 3. KNOWN P2/P3 BACKLOG ITEMS DIRECTLY RELEVANT HERE

Explicitly review:

### A. TransferDialog batch-selection gap

The UI/domain boundary previously had a known mismatch around selecting the batch for a stock transfer.

Resolve this if the existing inventory architecture supports a safe, narrow solution.

### B. PO / PR pagination

P2 intentionally left Purchase Orders / Purchase Requests using bounded `take: 100` behavior rather than proper operational pagination.

P3.8 is the correct phase to fix this.

### C. Multi-branch inventory adjustment UX

The existing adjustment workflow was branch-safe server-side but did not provide a user-selectable branch for authorized multi-branch users.

Assess and improve this using accessible branches only.

Do NOT allow arbitrary organization branch IDs.

---

# 4. WORKING METHOD

Inspect only:

* Products
* stock
* batches
* stock ledger
* adjustments
* transfers
* expiry
* suppliers
* purchase requests
* purchase orders
* goods receipts
* supplier invoices only where directly part of procurement handoff
* direct dependencies

Do NOT perform a whole-project audit.

Unrelated findings:

→ `BACKLOG.md`

Only proactively fix unrelated issues if there is immediate risk of:

* inventory corruption
* financial corruption
* patient-data corruption
* security/privacy breach
* destructive database behavior

---

# 5. FIRST — TRACE REAL INVENTORY FLOWS

Before changing code, trace actual existing workflows.

At minimum:

## A. Product stock lifecycle

Product
→ Batch
→ stock ledger
→ consumption
→ current balance

## B. Stock adjustment

Product/Batch
→ Adjustment
→ Ledger

## C. Stock transfer

Source Branch
→ Product/Batch
→ Transfer
→ Destination Branch
→ destination Batch/stock
→ Ledger

## D. Procurement

Purchase Request
→ Purchase Order
→ Goods Receipt
→ Batch
→ Stock Ledger
→ Supplier Invoice/AP

Document actual implementation before modifying it.

Do not assume models or states.

---

# 6. INVENTORY SOURCE OF TRUTH

Preserve the stock ledger as inventory truth.

Do not introduce direct UI-controlled quantity balances.

Every physical stock change should remain traceable through existing stock movement architecture.

Conceptually:

Product
→ Batch
→ StockLedgerEntry

with:

* branch
* quantity
* transaction type
* source/reference
* actor
* timestamp

where current schema supports it.

---

# 7. INVENTORY OVERVIEW

Review the existing inventory landing page.

Operational staff should quickly understand:

* product
* SKU
* category where supported
* branch
* available quantity
* low-stock state where existing reorder data supports it
* near-expiry
* expired stock
* stock value only if already available/appropriate

Do not turn this into executive analytics.

---

# 8. BRANCH CONTEXT

Inventory is branch-sensitive.

Make active/current branch context obvious.

For users authorized across multiple branches:

allow branch selection where operationally appropriate using:

`listAccessibleBranches`

or equivalent existing branch-safe functionality.

Never expose unrestricted organization-wide branch IDs.

---

# 9. PRODUCT DETAIL

Review Product detail/stock detail.

Useful operational context:

* SKU
* product name
* unit
* active/inactive
* selling price
* purchase cost where role-appropriate
* reorder level
* batches
* expiry
* quantity by batch
* recent ledger movement

Use actual fields.

Do not redesign Product master.

---

# 10. BATCH VISIBILITY

Batch-level stock is important.

Where applicable show:

* batch/lot number
* quantity
* expiry date
* received date
* branch/location
* status

Expired stock must be clearly distinguishable.

Do not present expired stock as available stock.

---

# 11. FEFO

P1 established FEFO.

Do NOT rebuild it.

Operational screens should make FEFO understandable where useful.

For example, batch lists can sort by earliest usable expiry.

Do not add a second FEFO algorithm in UI code.

---

# 12. NEAR-EXPIRY / EXPIRED STOCK

Review existing near-expiry and expired inventory workflows.

Operational staff should be able to identify:

* near-expiry batches
* expired batches
* remaining quantity
* product
* branch
* expiry date

Do not create medical/pharmaceutical regulatory disposal workflows yet.

Expired-batch write-off already exists.

Make it operationally usable if currently hidden or awkward.

---

# 13. KNOWN SMALL EXPIRY N+1

P2 left a small known issue:

> near-expiry/expired batch views call `getBalance` per batch.

This is directly in P3.8 scope.

Inspect it.

If still present:

replace it with a bounded/bulk balance approach if straightforward.

Do not over-optimize unrelated inventory queries.

---

# 14. STOCK LEDGER

Review the stock ledger operational view.

Staff should be able to understand:

* date/time
* product
* batch
* branch
* transaction type
* quantity in/out
* source/reference
* actor where supported

Preserve existing pagination.

Do not expose cryptic transaction codes without friendly labels where easy.

---

# 15. LEDGER FILTERS

Useful filters may include:

* branch
* product
* transaction type
* date range
* batch

Use existing capabilities.

Do not build an analytics query engine.

---

# 16. STOCK ADJUSTMENT

Review actual adjustment behavior.

Operational flow should clearly require:

* branch
* product
* batch where relevant
* quantity adjustment
* reason

Use actual model semantics.

Do not allow an adjustment to silently bypass batch tracking for batch-controlled stock.

---

# 17. MULTI-BRANCH ADJUSTMENT

Resolve the known UX limitation.

If the current server API already accepts a branch and verifies authorized branch access:

allow an authorized multi-branch user to choose from their accessible branches.

If the domain currently assumes active branch only:

inspect whether safely widening it is justified.

Do NOT weaken branch security.

A single-branch user should not see irrelevant branch selection complexity.

---

# 18. ADJUSTMENT DIRECTION / QUANTITY

Make semantics clear.

If the domain uses signed quantity:

UI should make it obvious whether stock is being:

* increased
* decreased

Do not make staff infer meaning from `+5` versus `-5` if a clearer control is possible.

Server remains authoritative.

---

# 19. NEGATIVE STOCK

Verify adjustments cannot unintentionally produce negative stock if current domain rules prohibit it.

Do not add client-only validation.

Test the server path.

---

# 20. STOCK TRANSFER

Trace the existing transfer architecture carefully.

Determine:

* source branch
* destination branch
* product
* source batch
* quantity
* destination batch behavior
* ledger entries
* transfer status/history

Do not redesign it before understanding it.

---

# 21. TRANSFERDIALOG BATCH GAP

Resolve the known TransferDialog issue.

The UI must not ask the server to transfer an ambiguous product quantity when the inventory architecture is batch-specific.

Preferred behavior:

Source branch
→ Product
→ available non-expired source batches
→ explicit batch selection
→ quantity
→ destination

Use existing batch records.

Do not fabricate batch IDs.

Do not select an expired batch.

If the server intentionally supports automatic FEFO allocation across multiple batches for transfers, document that and design the UI around the real behavior instead.

---

# 22. TRANSFER SECURITY

Verify:

* source branch authorized
* destination branch valid
* source != destination
* product belongs to organization
* batch belongs to source branch/product
* quantity valid

Never trust client-supplied branch/batch combinations.

---

# 23. TRANSFER ATOMICITY

A transfer must not:

* remove stock from source
* fail adding stock to destination
* leave half-completed state

Verify existing transaction behavior.

Fix if necessary.

---

# 24. CONCURRENT TRANSFERS

Test:

Two users attempt to transfer the same last units from a batch.

The system must not produce negative source stock.

Reuse existing row-lock/stock protections.

Do not build another locking subsystem.

---

# 25. DESTINATION BATCH SEMANTICS

Inspect actual architecture.

If destination stock preserves the same lot/batch number and expiry:

preserve it.

If the system creates a destination-branch batch record:

ensure provenance remains traceable.

Do not silently merge incompatible batches.

---

# 26. PURCHASE REQUEST

Review Purchase Request operational UX.

Useful fields:

* PR number
* requester
* branch
* date
* items
* quantities
* reason
* status
* approval state

Use actual model fields.

Do not create procurement policy.

---

# 27. PURCHASE REQUEST STATUS

Use actual lifecycle.

Do not invent states.

Actions should only appear when valid.

Examples conceptually may include:

Draft
Submitted
Approved
Rejected
Converted

but use real enums.

Server-side transition rules remain authoritative.

---

# 28. PR PAGINATION

Replace the known `take: 100` operational limitation with real pagination if still present.

Use the same pagination conventions already established in P2.

Preserve filters.

Do not return an unbounded list.

---

# 29. PURCHASE ORDER

Review PO workflow.

Useful context:

* PO number
* supplier
* branch
* order date
* items
* ordered quantity
* received quantity where supported
* price
* total
* status

Use existing models.

---

# 30. PO PAGINATION

Replace the known `take: 100` limitation with real pagination.

Use established P2 list conventions.

Do not introduce a one-off pagination pattern.

---

# 31. PR → PO HANDOFF

Trace how an approved PR becomes a PO.

If linked:

make traceability visible.

If they are currently independent:

do not invent a relationship.

Document actual behavior.

---

# 32. SUPPLIERS

Review Supplier operational pages only as required for procurement.

Useful information:

* supplier name
* contact
* active state
* payment terms where supported
* recent PO/AP context if already available

Do not turn P3.8 into supplier CRM.

---

# 33. PURCHASE ORDER APPROVAL

Inspect actual permissions/status transitions.

Do not broad-grant approval rights.

Operational UI should make:

* submit
* approve
* reject
* cancel

available only when valid and permitted.

Use real actions.

---

# 34. GOODS RECEIPT

This is a critical inventory boundary.

Trace:

PO
→ Goods Receipt
→ received quantities
→ ProductBatch
→ Stock Ledger

Verify:

* correct PO
* correct supplier
* correct branch
* quantity constraints
* batch/lot
* expiry where required
* purchase cost
* receiving actor
* timestamp

Use actual model.

---

# 35. PARTIAL RECEIPT

Determine whether partial PO receipt is supported.

If yes:

make:

Ordered
Received
Remaining

clear per line.

Do not mark PO fully received prematurely.

If not supported:

document the limitation.

---

# 36. OVER-RECEIPT

Determine current rule.

Do not invent one.

If receiving above PO quantity is prohibited:

server must reject it.

If allowed:

the behavior must be explicit and auditable.

---

# 37. GOODS RECEIPT ATOMICITY

Stock must not be increased unless the Goods Receipt succeeds.

Likewise:

Goods Receipt must not say received if batch/ledger creation fails.

Verify transaction boundary.

Fix if genuinely unsafe.

---

# 38. CONCURRENT RECEIVING

Test two users receiving the remaining PO quantity simultaneously.

Do not allow accidental over-receipt.

Use existing conditional/state guards where possible.

---

# 39. BATCH CREATION AT RECEIPT

Review batch behavior.

Where applicable:

receipt should capture:

* lot/batch number
* expiry
* quantity
* cost

Do not invent batch metadata unsupported by schema.

---

# 40. SUPPLIER INVOICE / AP HANDOFF

P1 already implemented supplier/AP behavior.

P3.8 should verify procurement handoff only.

Conceptually:

PO / Goods Receipt
→ Supplier Invoice
→ AP

Do not redesign AP.

Ensure procurement staff can understand whether a received PO has corresponding supplier financial documentation where current architecture supports it.

---

# 41. KNOWN SUPPLIER-INVOICE IDEMPOTENCY BACKLOG

P1 identified:

> concurrent identical `createSupplierInvoice` lacks client idempotency-key protection.

This is directly adjacent to procurement but may belong more naturally to P3.9.

Inspect it.

If Supplier Invoice creation is an operational step on the P3.8 procurement screen and realistic double-submit can create duplicate AP liabilities:

fix it narrowly using the established idempotency mechanism.

If not directly part of the operational workflow being touched:

leave it explicitly for P3.9.

Do not broaden scope.

---

# 42. FINANCIAL HANDOFF

Do not rebuild accounting.

Verify that:

* Goods Receipt
* Supplier Invoice
* stock valuation
* AP

continue to use existing financial posting/event architecture.

No direct journal creation from React/UI.

---

# 43. INVENTORY VALUATION

Do not redesign costing.

Use existing purchase cost / batch cost / COGS architecture.

P3.8 is not a costing-method project.

---

# 44. CANCELLATION / REVERSAL

Review only existing mechanisms.

Do not delete:

* Purchase Requests
* Purchase Orders
* Goods Receipts
* stock movements

that form historical records.

If cancellation exists:

preserve audit history.

Do not invent Goods Receipt reversal unless architecture already supports it.

---

# 45. ROLE PERMISSIONS

Review relevant seeded roles such as:

* Inventory / Store staff
* Procurement
* Pharmacist where stock visibility applies
* Accountant where AP visibility applies
* Clinic Manager/Admin

Use actual roles.

Do not broad-grant:

* finance administration
* accounting configuration
* settings
* clinical editing

to make inventory screens work.

Use permission-aware data loading.

---

# 46. BRANCH SECURITY

Explicitly inspect all P3.8 write paths.

Earlier P3 batches repeatedly found branch gaps.

Verify organization + branch enforcement for:

* adjustments
* transfers
* batch write-off
* PR
* PO
* goods receipt
* supplier-related operational writes

Never trust client-supplied branch IDs.

Cross-branch actions must be intentional and explicitly authorized.

A transfer is special:

source and destination are both branches.

Validate both according to existing organizational/access rules.

---

# 47. ERROR HANDLING

Operational errors should be understandable.

Examples:

* insufficient stock
* expired batch
* invalid source batch
* unauthorized branch
* same source/destination branch
* PO already received
* quantity exceeds remaining PO quantity
* stale approval state
* duplicate submission

Do not expose raw Prisma/PostgreSQL errors.

---

# 48. CONCURRENCY

Explicitly test realistic races:

* two adjustments against last stock where relevant
* two transfers of last stock
* two PO approval actions
* two Goods Receipts against same remaining quantity
* Supplier Invoice double-submit if touched

Use existing transaction/state-claim patterns.

Do not build speculative locking infrastructure.

---

# 49. PERFORMANCE

Preserve existing pagination.

Fix:

* PR pagination
* PO pagination
* known expiry-batch N+1

if still present.

Avoid:

* one stock-balance query per batch
* one supplier query per PO
* one product query per PR line

Use appropriate includes/selects/bulk aggregates.

Do not add Redis/caching.

---

# 50. RESPONSIVE / ACCESSIBILITY

Inventory/procurement screens should work on:

* desktop
* laptop
* warehouse/store tablet

Changed screens should have:

* clear quantity/unit labels
* visible branch context
* readable batch/expiry
* clear destructive actions
* confirmation for write-off/adjustment/cancellation where appropriate
* status not color-only
* accessible dialogs/errors

Do not site-wide redesign.

---

# 51. TESTING

Current baseline:

49 test files
324 tests
324 passing

Add targeted P3.8 tests.

Potential cases:

* batch balance visibility
* expired batch excluded from usable stock
* near-expiry bulk balance behavior
* authorized adjustment
* unauthorized cross-branch adjustment
* multi-branch adjustment selection/domain enforcement
* batch-specific transfer
* expired batch transfer blocked
* cross-branch transfer authorization
* concurrent transfer of last stock
* transfer ledger source/destination coherence
* PR pagination
* PR status transitions
* PO pagination
* PO approval
* Goods Receipt partial receipt
* over-receipt rejection
* concurrent receipt protection
* Goods Receipt creates correct batch/ledger
* procurement branch isolation
* Supplier Invoice idempotency if touched

Do not test CSS.

Do not weaken existing inventory/financial assertions.

Run full integration suite against local `his_test`.

---

# 52. BROWSER VERIFICATION

Use `his_dev`.

Perform realistic Inventory + Procurement workflows.

## Inventory

1. Login as appropriate inventory/store role.
2. Open Inventory.
3. Confirm active branch context.
4. Locate stocked product.
5. Open batch detail.
6. Confirm quantities/expiry.
7. Perform an authorized adjustment.
8. Confirm ledger movement.
9. For multi-branch authorized user, test branch selection if implemented.
10. Open Transfer.
11. Select source branch/product.
12. Select real available batch.
13. Transfer quantity to destination branch.
14. Confirm source reduction.
15. Confirm destination stock.
16. Confirm source/destination ledger entries.
17. Verify expired batch cannot be selected/transferred.
18. Review near-expiry/expired stock workflow.

## Procurement

19. Create Purchase Request.
20. Submit/approve according to actual lifecycle.
21. Create or reach Purchase Order through actual workflow.
22. Confirm supplier/items/quantities.
23. Approve/send according to actual lifecycle.
24. Receive part of the PO if supported.
25. Confirm remaining quantity.
26. Receive remaining quantity.
27. Confirm PO/receipt status.
28. Confirm ProductBatch creation/update.
29. Confirm stock ledger increase.
30. Confirm supplier/AP handoff where applicable.

## Pagination / Security / Integrity

31. Verify PR pagination.
32. Verify PO pagination.
33. Verify cross-branch adjustment denial.
34. Verify invalid transfer denial.
35. Verify concurrent last-stock transfer via automated test.
36. Verify concurrent over-receipt protection via automated test.
37. Verify Supplier Invoice duplicate protection if changed.

Clean walkthrough fixtures where practical.

---

# 53. REGULATORY FUTURE-PROOFING

Future markets:

* UAE
* Saudi Arabia
* Pakistan
* wider GCC

Future inventory/pharmacy regulatory needs may include:

* lot traceability
* serialization
* recall
* controlled medicines
* cold-chain tracking
* regulatory supplier records
* expiry/disposal records
* product registries

Do NOT implement them now.

Preserve clean provenance:

Supplier
→ PO
→ Goods Receipt
→ Product
→ Batch
→ Branch
→ Stock Ledger
→ Consumption / Transfer / Adjustment

with actor and timestamps where current architecture supports them.

---

# 54. PRODUCT BOUNDARY

P3.8 is NOT a warehouse-management-system project.

For V1 we need:

* reliable stock visibility
* batch/expiry visibility
* ledger
* adjustment
* transfer
* procurement request/order
* goods receipt
* supplier handoff
* pagination
* branch security
* concurrency safety

Do NOT expand into:

* barcode warehouse automation
* bin/location optimization
* pick-pack-ship
* route planning
* demand forecasting
* automated replenishment
* serialization
* cold-chain IoT
* supplier portals

unless an existing capability simply needs operational wiring.

---

# 55. DELIVERABLE

Create:

`P3_8_INVENTORY_PROCUREMENT_OPERATIONAL_UX_REPORT.md`

Include:

## Existing Inventory Workflow Traced

## Existing Procurement Workflow Traced

## Concrete Problems Found

## Improvements Implemented

## Inventory Overview

## Batch / Expiry

## Stock Ledger

## Adjustments

Explicitly report multi-branch adjustment behavior.

## Transfers

Explicitly report:

* TransferDialog batch-selection resolution
* source/destination batch behavior
* atomicity
* concurrency

## Purchase Requests

Include pagination.

## Purchase Orders

Include pagination.

## Goods Receipts

Include:

* partial receipt
* over-receipt behavior
* concurrency
* batch/ledger behavior

## Supplier / AP Handoff

## Supplier Invoice Idempotency Review

State whether fixed or deferred.

## Security / Branch Scoping

## Performance

Explicitly address the known expiry-batch N+1.

## Files Changed

## Tests

Report new + total.

## Browser Verification

## Remaining Inventory / Procurement Backlog

## Regression Status

Include:

* Prisma validate
* migration status
* typecheck
* lint
* integration tests
* production build

Integration DB:

Host:
Port:
Database:
Remote Supabase: NOT USED

Do not expose credentials.

---

# 56. STOP CONDITION

After P3.8 is complete:

STOP.

Do NOT begin P3.9.

Do NOT redesign Finance/Accounting.

Do NOT begin HR.

Do NOT begin Notifications.

Do NOT start P4.

Do NOT implement regulatory inventory integrations.

Return the P3.8 report for review.

The objective is:

> Make existing inventory and procurement capabilities operationally usable while preserving the stock ledger as inventory truth, maintaining batch/expiry integrity, preventing cross-branch or concurrent stock corruption, and keeping procurement connected to the existing AP/accounting architecture.
