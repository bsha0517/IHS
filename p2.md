# Claude Prompt — P2 Architecture, Performance & Data Quality Pass

Act as the Principal Software Architect, Senior Full-Stack Engineer, PostgreSQL/Prisma Database Architect, Performance Engineer, Healthcare Information Systems Architect, Security Engineer, and QA Engineer for the existing Healthcare Information System.

The system has already completed:

* Full build through Phase 14
* System Audit
* P0 Remediation
* P1 Clinical / Financial / Operational Integrity Remediation

Do NOT rebuild the system.

Do NOT repeat the full-system audit.

Do NOT reopen P0/P1 findings unless a P2 change causes a regression or you discover evidence that a previously-closed issue is actually still broken.

Read the existing project documentation first, especially:

* PROJECT_STATUS.md
* SYSTEM_AUDIT.md
* IMPROVEMENT_ROADMAP.md
* P0_REMEDIATION_REPORT.md
* P1_REMEDIATION_REPORT.md
* SYSTEM_INTEGRITY_MATRIX.md
* TRANSACTION_BOUNDARIES.md
* ARCHITECTURE.md
* DATABASE.md
* SECURITY.md
* API.md
* DEPLOYMENT.md

The purpose of this pass is:

**Improve architecture consistency, database quality, performance, scalability, observability, maintainability, and data-quality protections without redesigning the application or adding major new product features.**

---

# 1. P2 SCOPE CONTROL

Work ONLY on P2-class issues.

Do not start:

* CRM / Leads
* major Patient 360 redesign
* Reception redesign
* Doctor workspace redesign
* new LIS/PIS/RIS features
* WhatsApp/SMS provider integrations
* PACS
* large UI redesign
* full compliance certification
* major infrastructure changes
* microservices
* Kafka/RabbitMQ
* speculative architecture

If you find unrelated improvements, add them to:

`BACKLOG.md`

Do not fix them unless they create an immediate security/data-integrity problem.

---

# 2. FIRST: VERIFY CURRENT P2 FINDINGS

Before modifying code, inspect whether each historical P2 issue still exists after the P0/P1 passes.

Do not assume the old audit is still accurate.

Create:

`P2_FINDINGS.md`

For every item classify:

* CONFIRMED
* ALREADY FIXED
* PARTIALLY FIXED
* NO LONGER APPLICABLE
* NEWLY DISCOVERED

Keep this document concise.

Then implement confirmed P2 items.

---

# 3. DATABASE INDEX REVIEW

The original audit identified missing high-value indexes including:

* `Invoice.issuedAt`
* `Payment.receivedAt`
* `Charge.createdAt`

However Phase 14 and subsequent remediation may already have added some indexes.

Inspect the actual current Prisma schema and PostgreSQL indexes.

Do NOT blindly add indexes.

Review real query patterns for:

* Patient
* Appointment
* Encounter
* ClinicalOrder
* Charge
* Invoice
* Payment
* Refund
* Claim
* Journal
* JournalLine
* ProductBatch
* StockLedgerEntry
* SupplierInvoice
* GoodsReceipt
* PayrollRun
* CommissionAccrual
* Notification
* OutboxEvent
* AuditLog
* ClinicalAccessLog

For high-volume tables, verify appropriate indexes for common combinations such as:

organizationId + date

branchId + date

patientId + date

providerId + date

status + date

referenceType + referenceId

invoiceId

encounterId

clinicalOrderId

productId + branchId

batchId

supplierId

Do not add indexes merely because a foreign key exists.

Avoid over-indexing.

If possible, use actual SQL `EXPLAIN` / `EXPLAIN ANALYZE` against representative queries where useful.

Document meaningful additions in DATABASE.md.

---

# 4. COMMISSION PERFORMANCE / N+1

The original audit found provider commission accrual could execute roughly 90 sequential queries during POS/payment processing.

Inspect the current implementation after P1.

Determine whether an N+1 pattern still exists.

Look especially at:

* commission rule resolution
* provider rules
* service rules
* product rules
* organization defaults
* collected-revenue calculations
* invoice-line iteration
* payment allocation iteration

If confirmed:

Replace repeated sequential lookups with efficient prefetching/batching.

Prefer:

* one query for applicable rules
* maps/indexes in memory
* grouped calculations
* bulk inserts where safe

Do NOT weaken the existing commission precedence logic.

Do NOT change commission calculation semantics.

Add tests proving results before/after remain identical.

Measure query count or provide other evidence of improvement if practical.

---

# 5. BATCH-AWARE INVENTORY ADJUSTMENTS

The original audit reported that manual inventory adjustments were batch-blind.

Inspect the current implementation.

For batch-tracked products, manual adjustments should not create ambiguous inventory.

Design the workflow so stock adjustments can identify:

* product
* branch
* batch
* quantity
* reason
* adjustment type
* user
* timestamp
* reference

For stock reduction:

Require selection of a real batch where appropriate.

For additions:

Allow either:

* adjustment against an existing batch

or

* creation/selection of an explicitly identified batch depending on the business case.

Do not allow batch-level stock to become mathematically inconsistent with the stock ledger.

Preserve the stock ledger as the source of truth.

Keep FEFO consumption logic unchanged.

Add tests for:

* batch increase
* batch decrease
* insufficient batch balance
* expired batch adjustment
* multi-batch product
* audit/reference integrity

---

# 6. ACCOUNTING AUDITABILITY

The audit previously found accounting postings lacked an independently useful audit surface.

Inspect current behavior.

Every significant accounting action should be traceable to:

* triggering business transaction
* journal
* journal lines
* posting reference
* user/system actor where available
* timestamp
* branch
* organization
* reversal relationship where applicable

Do NOT duplicate the journal ledger into another accounting database.

Instead ensure the existing journal/reference architecture is traceable.

If needed, add an admin/accountant-facing read-only viewer allowing:

Business Transaction
→ Journal
→ Journal Lines
→ Source Reference

Examples:

Invoice
Payment
Refund
Goods Receipt
Supplier Invoice
Supplier Payment
Stock Adjustment
Asset Acquisition
Payroll Approval
Payroll Payment
Package Recognition
Manual Journal

Do not allow editing from this viewer.

---

# 7. CLINICAL ACCESS LOG VIEWER

P0/P1 strengthened clinical audit controls, but the audit identified lack of a usable admin viewer.

Inspect current implementation.

If not already available, create a restricted admin/security audit page for:

`clinical_access_log`

Support filtering by:

* date range
* user
* patient
* encounter if available
* action
* organization
* branch where applicable

Display:

* who accessed
* what patient/clinical record
* action
* timestamp
* relevant context

Do not expose this page to ordinary clinical staff.

Use existing RBAC conventions.

Do not modify clinical records from this interface.

Also review whether important clinical read paths still completely bypass `writeClinicalAccessLog`.

Do NOT attempt to log every tiny query.

Prioritize meaningful chart/result/note access.

---

# 8. PAGINATION OF LARGE DATASETS

Review all major list/report screens for unbounded or hard-capped queries.

Known historical concerns include:

* invoice list hard cap
* stock ledger hard cap
* reports pulling complete history
* large patient/appointment/payment lists

Implement server-side pagination where appropriate.

Prioritize:

Patients
Appointments
Invoices
Payments
Charges if exposed
Refunds
Claims
Stock Ledger
Goods Receipts
Supplier Invoices
Journals
Audit Logs
Clinical Access Logs
Employees
Assets
Communications
Notifications
System Events

Use consistent pagination conventions.

Preferred pattern:

* page
* pageSize
* totalCount
* totalPages

For very large append-only logs, cursor pagination is acceptable if materially better.

Do not load thousands of rows and paginate only in the browser.

Preserve filters across page navigation.

---

# 9. REPORT SCALABILITY

Review reports that currently recompute large history every request.

Pay attention to:

* Revenue
* Collections
* Cash
* AR
* AP
* Inventory valuation
* COGS
* P&L
* Trial Balance
* Balance Sheet
* Stock movement
* Provider performance
* Commission reports

Do NOT introduce materialized views merely because they are possible.

First optimize:

* indexes
* query shape
* aggregation
* unnecessary repeated queries
* date filtering
* branch filtering

If a report is already efficient enough for the expected V1 scale, leave it alone.

Document queries likely to become problematic at much larger scale in:

`PERFORMANCE_NOTES.md`

---

# 10. ACCOUNTMAPPING INTENT TYPE

The old audit identified:

`AccountMapping.intent`

as potentially being a generic String.

Inspect the current schema.

If it remains an unrestricted string but the application already operates on a closed PostingIntent taxonomy, migrate it safely to a constrained representation.

Options:

* Prisma/PostgreSQL enum
* DB CHECK constraint

Choose whichever best fits the current architecture.

Do not break existing mappings.

Migration must validate existing values first.

Add tests preventing invalid posting intents.

---

# 11. UNIQUENESS AND DATA QUALITY

Review whether these historical findings remain:

* `User.username`
* `Provider.licenseNumber`

Also inspect other identifiers that logically need uniqueness.

Examples:

within organization:

* provider license where appropriate
* employee number
* SKU
* service code
* medication/product code
* lab test code
* imaging service code

Do NOT blindly make globally unique fields that should only be organization-scoped.

Use composite unique constraints when appropriate:

`organizationId + value`

Before migration:

check for duplicate existing data.

If duplicates exist:

do NOT silently delete or merge them.

Report them and create a safe migration/remediation path.

---

# 12. MISSING UPDATEDAT FIELDS

Historical audit noted missing `updatedAt` consistency on financial entities such as:

* Invoice
* Charge
* Payment

Inspect current schema.

For mutable business records, determine whether `updatedAt` is useful and semantically correct.

Do not add it to immutable append-only ledger/history records merely for uniformity.

For mutable operational records where it adds traceability, add:

`updatedAt @updatedAt`

Apply migrations safely.

---

# 13. BRANCH CONSISTENCY IN CLINICAL DATA

Review clinical entity branch scoping after P0/P1.

Historical audit found inconsistent branch columns among clinical siblings.

Inspect:

Episode
Encounter
VitalSign
ClinicalNote
Diagnosis
ClinicalOrder
Prescription
Lab execution records
Imaging records
Dispensing records

Do NOT duplicate branchId everywhere automatically.

For each model decide whether branch is:

A. authoritative direct field

or

B. correctly inherited from a parent relation.

The key requirement is:

No ambiguous branch ownership.

No cross-branch access leakage.

No possibility that child branch conflicts with parent branch.

If a child stores branchId redundantly, ensure consistency is enforced.

Prefer derivation where direct storage has no useful independent meaning.

Document the final model in DATABASE.md.

---

# 14. ACTOR FOREIGN KEYS

The original audit identified many `createdBy`, `updatedBy`, `approvedBy`, `verifiedBy`, etc. fields that stored user IDs without proper relational FKs.

Inspect current schema.

Do NOT attempt a reckless migration of every historical actor field at once.

Classify actor columns:

A. Should have FK to User and user deletion is restricted

B. Should deliberately preserve a historical string/UUID without FK

C. System actor / external actor

For high-value clinical/financial/security records, add real relations where safe.

Examples:

* approvedBy
* verifiedBy
* finalizedBy
* authorizedBy
* createdBy
* receivedBy
* dispensedBy
* refundedBy
* paidBy

Important:

A historical financial/clinical record must survive account deactivation.

Prefer deactivation of users rather than deletion.

Use `onDelete: Restrict` or appropriate preservation behavior.

Document any actor columns intentionally left without FK and why.

---

# 15. PROCEDURE EVENT ALIGNMENT

The original audit said `ProcedureCompleted` was documented but not actually fired/registered, with procedure billing partly manual.

Inspect current architecture.

Do NOT blindly add an event just because it appears in an old document.

Determine:

* whether procedures currently have a genuine execution lifecycle
* whether procedure completion is represented by ClinicalOrder status
* whether service billing/stock consumption already occurs through another legitimate workflow
* whether `ProcedureCompleted` remains architecturally useful

If the event is genuinely required:

implement it once, idempotently.

If current architecture makes it unnecessary:

update documentation to remove misleading references.

Do not create duplicate billing or inventory side effects.

---

# 16. STRUCTURED SERVER LOGGING

The original audit found no structured server logging.

Add lightweight structured application logging suitable for production troubleshooting.

Do not add a huge observability platform.

Create a centralized logger abstraction.

Logs should support structured fields such as:

* timestamp
* level
* event
* request or correlation id where available
* organizationId
* branchId
* userId
* domain
* operation
* entityId/reference
* error class/message

Never log:

* passwords
* reset tokens
* session tokens
* full clinical notes
* sensitive patient details unnecessarily
* card data
* secrets
* DATABASE_URL

Use it for meaningful events such as:

* unexpected domain failures
* failed outbox processing
* dead-letter events
* accounting posting failures
* integration/provider failures
* authorization anomalies
* database/runtime errors

Avoid noisy logging for every ordinary CRUD operation.

If no external logger is configured, structured JSON console output is acceptable as the initial implementation.

Document extension points for future Sentry/Datadog/etc. without integrating a vendor now.

---

# 17. GLOBAL QUERY / RAW PRISMA CONSISTENCY

The audit previously identified raw Prisma usage inside route/page code such as the queue page.

Review Server Components and route handlers for business queries executed directly from UI files.

Do NOT create abstraction for abstraction's sake.

But if business rules, branch scoping, authorization, or reusable query logic are being implemented directly in pages/components:

move them into the proper domain/service layer.

UI files should not independently recreate:

* branch authorization
* patient visibility
* clinical access rules
* invoice math
* stock calculations
* accounting logic
* appointment conflict logic

Run a focused search for Prisma imports under:

`src/app`

Classify each as:

* legitimate simple read
* should move to domain service

Fix the unsafe/redundant ones.

---

# 18. TAX LOOKUP PERFORMANCE

Historical audit noted repeated `getTaxRate` calls per invoice line.

Inspect current invoice generation.

If tax rules are queried separately for each invoice line:

prefetch relevant tax rules once.

Build an in-memory lookup map.

Preserve exact existing precedence:

specific service/product rule
→ organization default
→ zero

Do not change tax calculation or rounding behavior.

Add regression tests proving identical invoice totals.

---

# 19. LARGE COMPONENT / FILE REVIEW

Do a limited maintainability pass.

Identify only genuinely problematic oversized files/components that:

* mix unrelated responsibilities
* are difficult to test
* duplicate business logic
* cause repeated rerenders
* have become dangerous to modify

Do NOT spend the entire P2 pass splitting files just to reduce line count.

Refactor only high-value cases.

Business behavior must stay unchanged.

---

# 20. DEAD CODE REVIEW

Identify:

* unused service functions
* abandoned actions
* duplicate utilities
* obsolete TODO/FIXME comments
* old phase placeholders that contradict current implementation
* dead navigation items pointing to implemented functionality elsewhere

Do not remove code merely because a static search finds no caller if it is part of an intentional public/domain API.

For confirmed dead code:

remove safely.

Update PROJECT_STATUS.md when a historical statement is now misleading.

---

# 21. DATA RETENTION / DELETION CONSISTENCY

P0 replaced dangerous cascade deletes with restrictive historical protection.

Review whether remaining delete actions produce confusing dead ends.

For important entities, ensure users have sensible operational alternatives:

* deactivate
* cancel
* void
* reverse
* archive
* entered-in-error
* amendment

Do NOT redesign lifecycle models.

Only fix obvious cases where the UI still exposes delete but the DB now correctly refuses it with a raw error.

Translate those into user-friendly actions/messages.

---

# 22. PERFORMANCE BASELINE

Create a lightweight performance baseline.

Use representative development/test data and document approximate behavior for:

* Patient search
* Appointment day view
* Patient 360
* Encounter load
* POS pending charges
* Invoice list
* Stock ledger
* Inventory report
* Journal list
* Trial Balance
* Management dashboard
* Reports

Where possible record:

* query count
* obvious N+1 behavior
* approximate query duration
* returned row count

Do not fabricate benchmark numbers.

If the environment cannot produce meaningful timing data, report query-shape findings instead.

Create:

`PERFORMANCE_BASELINE.md`

---

# 23. PREPARATION FOR 200+ USERS

This is NOT a full production scaling pass yet.

However, inspect whether any obvious architectural decisions would break under roughly 200+ staff users.

Focus on:

* database connection usage
* Prisma connection configuration
* Supabase pooler usage
* synchronous outbox dispatch behavior
* long transactions
* excessive sequential queries
* full-table scans
* polling loops
* unbounded lists
* repeated dashboard recomputation

Do NOT migrate to microservices.

Do NOT add Redis merely because caching exists.

Do NOT introduce a message broker.

Document what is acceptable now and what should be addressed later under production-readiness work.

---

# 24. TESTS REQUIRED

Add or strengthen automated tests for every P2 behavior changed.

At minimum cover any implemented changes to:

* batch-aware adjustments
* posting intent constraints
* uniqueness constraints
* actor FK behavior
* branch inheritance/consistency
* pagination
* tax batching
* commission batching
* clinical access audit
* procedure event if implemented
* accounting traceability

Regression suite must continue to cover P0/P1 functionality.

Run:

* Prisma validation
* migrations
* typecheck
* lint
* tests
* production build

Do not skip or suppress failing tests.

If an existing test is wrong because architecture intentionally changed, update it and explain why.

---

# 25. REQUIRED DELIVERABLES

At the end create/update:

### `P2_FINDINGS.md`

Confirmed status of historical P2 findings before changes.

### `P2_REMEDIATION_REPORT.md`

For each item:

Finding
Current status
Root cause
Implementation
Files changed
Migration if any
Tests
Performance impact
Remaining risk
Final status

### `PERFORMANCE_BASELINE.md`

Current performance/query baseline.

### `BACKLOG.md`

Only for unrelated findings discovered during P2.

Update if necessary:

* PROJECT_STATUS.md
* DATABASE.md
* ARCHITECTURE.md
* SECURITY.md
* API.md
* DEPLOYMENT.md

Do not rewrite documentation that has not materially changed.

---

# 26. FINAL VERIFICATION QUESTIONS

At the end answer these questions clearly:

## A.

Are any major list/report screens still loading unbounded datasets or relying on arbitrary hard caps?

## B.

Do any high-frequency workflows still contain significant N+1 query patterns?

## C.

Can manual inventory adjustments create batch-level ambiguity or mismatch?

## D.

Can an accountant trace a journal posting back to its operational source and vice versa?

## E.

Can authorized administrators meaningfully review clinical access history?

## F.

Are organization/branch boundaries still correctly enforced after all P2 refactoring?

## G.

Are database indexes aligned with actual query patterns rather than blindly indexing every FK?

## H.

Are major identifiers and posting intents protected from invalid duplicate/free-form data where appropriate?

## I.

Does the application now have useful structured server-side logging without exposing sensitive data?

## J.

Are there any architectural/performance issues discovered in P2 that would materially block a deployment supporting approximately 200 concurrent/active staff users?

## K.

Did any P2 change regress P0/P1 protections?

## L.

Is the codebase ready to proceed to P3 workflow/maintainability work?

---

# 27. IMPORTANT WORKING RULES

Do not repeatedly explain what you are about to do.

Do not create a new architecture.

Do not reopen finished work without evidence.

Do not chase unrelated improvements.

Do not make speculative changes.

Inspect relevant code.

Implement.

Test.

Document concisely.

Stop when P2 is complete.

Do NOT begin P3.
