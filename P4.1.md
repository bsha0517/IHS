# CLAUDE MASTER PROMPT — P4 COMMERCIAL DEPLOYMENT READINESS

## Healthcare Information System

P0, P1, P2 and P3 are COMPLETE.

P3 ended with successful cross-role acceptance across:

* Reception
* Nursing
* Doctor
* Laboratory
* Radiology
* Pharmacy
* Billing / Cashier
* Inventory / Procurement
* Accounting
* HR / Payroll
* Notifications
* Admin / Settings
* Multi-branch isolation

Current verified baseline:

* Local PostgreSQL operational
* Development DB: `his_dev`
* Integration test DB: `his_test`
* Remote Supabase not used by integration tests
* 34/34 migrations applied
* Prisma validation clean
* TypeScript clean
* Lint clean
* Production build clean
* 55 integration test files
* 448 integration tests
* 448/448 passing
* 66 application routes

P3 acceptance decision:

> P3 Operational UX & Workflow Polish is complete.

P4 now begins.

---

# 1. PURPOSE

P4 is:

# COMMERCIAL DEPLOYMENT READINESS

P4 is NOT another operational workflow phase.

Do NOT reopen P3 workflows unless P4 exposes a genuine production-readiness defect.

The objective is:

> Prepare the HIS to be deployed and operated as a real commercial SaaS / hosted clinic platform with reliable environments, backups, security controls, monitoring, migrations, onboarding, performance validation, and operational procedures.

P4 should move the system from:

"functional V1 application"

to:

"deployable and supportable commercial product."

---

# 2. TARGET DEPLOYMENT CONTEXT

The product is intended eventually for:

* Pakistan
* UAE
* Saudi Arabia
* wider GCC

Initial deployment may support:

* single clinic organizations
* multi-branch clinics
* 200+ staff/users
* concurrent Reception / clinical / pharmacy / billing / inventory / HR usage

Do NOT implement country-specific regulatory adapters yet.

Regulatory work comes in a later dedicated phase.

---

# 3. P4 PHASE STRUCTURE

P4 should be executed in batches.

Recommended sequence:

## P4.1 — Production Environment & Deployment Architecture

## P4.2 — Database Reliability / Backup / Restore / Disaster Recovery

## P4.3 — Production Security Hardening

## P4.4 — Observability / Logging / Error Monitoring / Operational Health

## P4.5 — Performance / Concurrency / Load Validation

## P4.6 — Data Import / Clinic Onboarding / Initial Setup

## P4.7 — Reporting / Export / Operational Data Portability

## P4.8 — Release / Migration / Upgrade Safety

## P4.9 — Commercial Readiness Acceptance

STOP after each batch and return a report.

Do not implement the entire P4 in one huge change.

---

# 4. P4.1 — PRODUCTION ENVIRONMENT & DEPLOYMENT ARCHITECTURE

Start with P4.1 ONLY.

The purpose is to establish a real production deployment architecture for the existing Next.js/PostgreSQL system.

Do NOT begin P4.2 yet.

---

# 5. FIRST — TRACE CURRENT DEPLOYMENT ASSUMPTIONS

Inspect only relevant files and direct dependencies.

Trace:

* package.json
* Next.js config
* Prisma config
* environment variable usage
* DB connection implementation
* authentication/session configuration
* email adapter configuration
* cron/outbox scheduling
* file/document storage if any
* deployment-specific scripts
* logging/instrumentation
* production build assumptions
* Docker/container files if present
* Vercel configuration if present
* Supabase configuration if present
* CI configuration if present

Do NOT perform a whole-project audit.

---

# 6. ENVIRONMENT INVENTORY

Identify all environment variables currently used.

Classify them:

## Required production

Examples conceptually:

* DATABASE_URL
* DIRECT_URL
* authentication secrets
* base URL
* email provider
* storage provider

## Optional

## Development-only

## Test-only

## Deprecated/unused

Do not expose secret values in the report.

---

# 7. ENV VALIDATION

Determine whether the application validates required environment variables at startup.

If not:

implement a small centralized environment validation layer.

Requirements:

* fail fast in production
* clear missing-variable names
* no secrets logged
* test environment remains usable
* development remains practical

Do not introduce a large configuration framework unnecessarily.

---

# 8. DATABASE CONNECTION ARCHITECTURE

Review current PostgreSQL connection approach.

Previous work established:

* Prisma
* PostgreSQL
* Supabase-compatible transaction pooler considerations
* pg Pool max 3 in one direct pool usage
* runtime DB role separation

Verify the actual current configuration.

Document separately:

## Application runtime connection

## Prisma migration/direct connection

Do not let migrations run through a transaction pooler if unsupported.

---

# 9. RUNTIME DATABASE ROLE

P1 established restricted runtime DB role:

`avant_app_runtime`

Verify production architecture preserves:

Application
→ restricted runtime DB user

Prisma migrations/admin CLI
→ owner/direct privileged DB user

Do not allow the web application runtime to use DB-owner credentials.

---

# 10. DEPLOYMENT PLATFORM DECISION

Assess actual hosting requirements for this stack.

Primary architecture should support approximately:

200+ staff users

with:

* Next.js application
* PostgreSQL
* scheduled outbox processing
* transactional DB operations
* secure environment secrets
* logs
* monitoring

Compare the practical fit of:

* Vercel + managed PostgreSQL
* Railway
* Render
* Fly.io
* AWS
* Azure
* DigitalOcean
* similar providers only if directly relevant

Do NOT migrate platforms merely for preference.

The deliverable should recommend a realistic initial production architecture and a scale-up path.

---

# 11. IMPORTANT — SCHEDULED JOBS

Current architecture uses an Outbox.

Existing concern:

outbox recovery sweep may only run daily under Vercel Hobby cron.

Commercial production requires a more appropriate schedule.

Determine current job entry points.

Design a production scheduler for:

* pending outbox dispatch
* stale processing recovery
* dead-letter handling/retry where appropriate

Target approximately:

every few minutes

not once per day.

Do not build Kafka/Redis merely for this.

---

# 12. OUTBOX EXECUTION MODEL

Decide whether production should use:

A. platform cron calling protected application endpoint

B. dedicated worker process

C. scheduled server function

Use the simplest architecture that reliably supports current volume.

Document tradeoffs.

Do not create a second event system.

---

# 13. CRON ENDPOINT SECURITY

If cron uses an HTTP endpoint:

require:

* secret/token verification
* non-public execution semantics
* safe response
* no sensitive output
* idempotent execution

Do not expose an unauthenticated "run jobs" endpoint.

---

# 14. APP BASE URL

Review whether absolute URLs are generated anywhere.

Define one clear production:

`APP_BASE_URL`

or existing equivalent.

Validate scheme/host.

Do not hardcode localhost or deployment domains.

---

# 15. AUTH COOKIE CONFIGURATION

Review session cookies.

Production expectations:

* HttpOnly
* Secure in production
* SameSite appropriate
* path correct
* sensible expiration
* no session token leakage to client JavaScript

Do not redesign authentication.

---

# 16. PROXY / HTTPS

Ensure deployment assumptions correctly handle:

* HTTPS termination
* reverse proxy headers
* secure cookie detection
* application origin

Do not trust arbitrary forwarded headers for security-sensitive host decisions.

---

# 17. PASSWORD RESET EMAIL

P0 confirmed reset functionality exists.

Actual SMTP/provider configuration remains deployment work.

P4.1 should establish a real production adapter/configuration pattern.

Do not hardcode one vendor unless current architecture strongly suggests one.

Potential providers:

* Resend
* Postmark
* SendGrid
* SES

Use a clean adapter.

No marketing email system.

---

# 18. EMAIL FAILURE

Password-reset email failure should:

* not expose whether a user exists
* produce structured server-side error/logging
* not crash unrelated application flows

Preserve security semantics.

---

# 19. DOCUMENT / FILE STORAGE

Trace whether Documents or uploads currently persist:

* DB
* local filesystem
* object storage
* external service

Local ephemeral filesystem must NOT be considered durable production storage.

If file uploads currently rely on local disk:

design/implement an abstraction suitable for object storage.

Potential:

* S3
* Cloudflare R2
* Supabase Storage
* compatible object storage

Do not build a full document management rewrite.

---

# 20. STORAGE SECURITY

If object storage is involved:

* private by default for patient/HR documents
* short-lived signed access
* organization ownership validation
* safe MIME/type restrictions where already relevant

Do not create public patient document URLs.

---

# 21. BUILD / START COMMANDS

Ensure documented production commands exist for:

* install
* Prisma generate
* migration deploy
* build
* start

Avoid `prisma migrate dev` in production.

Use:

`prisma migrate deploy`

or equivalent existing correct command.

---

# 22. MIGRATION DURING DEPLOYMENT

Establish the expected order:

1. backup
2. deploy migration
3. application deployment
4. health verification

or another justified safe sequence.

P4.8 will harden upgrades further.

P4.1 only needs a coherent baseline deployment process.

---

# 23. HEALTH CHECK

Determine whether application currently exposes a production health endpoint.

If not, create a minimal one.

Potential:

`/api/health`

Requirements:

* application reachable
* DB reachable
* no patient data
* no secret data
* no raw DB error dump

Possible output:

status
database
timestamp/version

Do not run heavy queries.

---

# 24. READINESS VS LIVENESS

If useful for target deployment platform, distinguish:

* liveness: application process responds
* readiness: database connectivity available

Do not overengineer Kubernetes-style probes if platform does not need them.

---

# 25. VERSION / RELEASE IDENTIFICATION

Introduce a simple mechanism to identify deployed version.

Examples:

* git commit SHA env
* release version env
* package version

Expose only non-sensitive version metadata in health/debug context.

This will help production support.

---

# 26. CI BASELINE

Inspect current CI configuration.

If absent, add a simple production-quality CI workflow.

At minimum on pull request / main:

* install
* Prisma generate
* typecheck
* lint
* integration tests
* production build

Use a local/CI PostgreSQL service for integration tests.

Do NOT point CI tests at production or shared development DB.

---

# 27. CI DATABASE ISOLATION

CI should create/use a disposable PostgreSQL database.

Apply migrations cleanly.

Do not rely on `his_dev`.

Do not use remote Supabase for integration tests.

---

# 28. DEPLOYMENT SECRETS

Document where production secrets should live:

* hosting platform secret manager
* CI secret store

Never:

* committed `.env`
* plaintext source files
* logs

Do not expose example real credentials.

---

# 29. ENVIRONMENT SEPARATION

Define:

## Development

Local/dev DB

## Test

Disposable/local/CI DB

## Staging

Production-like isolated environment

## Production

Live customer data

Staging must not use production patient data unless explicitly anonymized later.

---

# 30. STAGING ENVIRONMENT

Recommend a staging environment before production.

Staging should support:

* migration verification
* smoke testing
* job scheduler testing
* email adapter testing
* object storage testing
* deployment validation

Do not require identical production scale initially.

---

# 31. SEEDING

Review `prisma/seed.ts`.

Production deployment must NOT blindly seed:

* demo patients
* demo employees
* demo financial data

Separate:

* required system permissions/default roles
  from:
* development fixtures

If current seed mixes them, refactor appropriately.

---

# 32. BOOTSTRAP ADMIN

Design a safe production method for creating the first organization/admin.

Possible:

* explicit bootstrap script
* one-time setup flow
* CLI command

Do not leave publicly accessible "create first super admin" functionality after initialization.

---

# 33. MULTI-TENANT DEPLOYMENT ASSUMPTION

Current architecture is:

multiple organizations in one database.

Preserve that unless there is a proven blocker.

Do NOT convert to:

database per tenant

during P4.1.

Document future scaling options only.

---

# 34. DATABASE REGION / APP REGION

For commercial deployments, application and DB should be geographically close.

Document this.

For future GCC/Pakistan deployments, regulatory/data-residency decisions will later influence region selection.

Do not hardcode country residency policy now.

---

# 35. TIMEZONE HANDLING

Organization has:

`defaultTimezone`

Branch has:

`timezone`

Review deployment-level time assumptions.

Servers/DB should generally operate predictably, preferably UTC internally where current application semantics support it.

Do not rewrite date logic here.

Only fix environment-dependent timezone bugs if found.

---

# 36. CURRENCY

Organization has `defaultCurrency`.

Do not introduce country tax/currency rules.

Verify production deployment does not depend on server locale for financial calculations.

---

# 37. NODE / PACKAGE VERSION

Define supported runtime versions.

Prefer current LTS Node compatible with existing Next.js stack.

Ensure:

* `.nvmrc`
* engines field
* or equivalent

if currently absent and useful.

Do not upgrade major framework versions in P4.1 unless required for production support/security.

---

# 38. LOCKFILE

Ensure production installs use the committed lockfile.

Use deterministic installation:

`npm ci`

where appropriate.

Do not regenerate dependencies unnecessarily.

---

# 39. DOCKER — DECISION

Evaluate whether Docker is useful for:

* Railway/Render/Fly/AWS style hosting
* reproducible deployments
* worker process

If current deployment target does not need Docker:

do not add it merely because production systems often use Docker.

Document decision.

---

# 40. HORIZONTAL SCALING

Current application should remain stateless except for DB/session records.

Verify no critical runtime state lives only in process memory.

If multiple app instances run:

* authentication should work
* branch session should work
* notification state should work
* outbox processing should remain concurrency-safe

Do not introduce sticky sessions unnecessarily.

---

# 41. PRISMA CLIENT SINGLETON

Verify Prisma client lifecycle is safe for:

* Next.js development
* serverless/production
* multiple instances

Do not instantiate excessive clients.

---

# 42. CONNECTION POOLING

Commercial deployment with 200+ staff must avoid DB connection exhaustion.

Document:

* application connection pool strategy
* max connections per instance
* connection pooler if used
* direct migration connection

Do not guess arbitrary enormous pool sizes.

---

# 43. SERVERLESS VS LONG-LIVED SERVER

Explicitly evaluate current architecture against:

## Serverless

advantages/disadvantages for:

* Prisma connections
* scheduled jobs
* long transactions
* outbox worker

## Long-lived Node instance

advantages/disadvantages.

Provide a recommendation based on THIS codebase.

---

# 44. LONG TRANSACTIONS

P2 found multiple transactions with up to ~20-second limits.

Do not optimize them all now.

Document that production platform must support these transaction characteristics.

If serverless timeout constraints would realistically break them, flag that in deployment architecture.

---

# 45. BACKGROUND WORKER OPTION

If recommending a long-lived deployment:

consider separating:

Web
Worker

Worker handles:

* outbox dispatch
* stale recovery

using the same code/database.

Do not build unrelated background services.

---

# 46. ERROR PAGES

Check production handling for:

* 404
* unauthorized/forbidden
* unexpected server error

No stack traces/secrets should be exposed in production.

Do not redesign UI.

---

# 47. SOURCE MAP / DEBUG DATA

Avoid exposing sensitive server-side source data publicly.

If platform supports source maps for error monitoring, keep them private where possible.

Detailed monitoring comes P4.4.

---

# 48. PRODUCTION LOG LEVEL

Define:

development:
more verbose

production:
structured operational logs

Never log:

* passwords
* reset tokens
* session tokens
* raw clinical result payloads unnecessarily
* full patient records

Detailed log hardening will come P4.4.

---

# 49. FEATURE FLAGS

Do not build a feature-flag framework in P4.1.

Environment gating for true infrastructure features is acceptable.

---

# 50. DEPLOYMENT DOCUMENTATION

Create a concise deployment guide:

`docs/PRODUCTION_DEPLOYMENT.md`

It should include:

* architecture diagram in text/Mermaid if current docs allow
* environment variables
* database setup
* migrations
* runtime DB role
* app deployment
* worker/cron setup
* email setup
* storage setup if applicable
* health check
* initial admin bootstrap
* staging
* rollback basics
* common deployment failures

No secrets.

---

# 51. ARCHITECTURE RECOMMENDATION

The final report should recommend:

## Initial commercial deployment

Suitable for first real clinics.

## Scale-up deployment

Suitable when customer count / concurrency grows.

Do NOT recommend enterprise complexity immediately.

---

# 52. COST AWARENESS

Provide rough architecture-level cost categories only:

* application hosting
* PostgreSQL
* object storage
* email
* monitoring
* backups

Do not hardcode vendor prices unless current and explicitly researched.

No web research is required in this coding phase unless specifically requested.

---

# 53. DO NOT BEGIN P4.2

P4.2 will handle:

* backup strategy
* restore testing
* PITR
* disaster recovery
* retention
* RPO/RTO
* DB maintenance

Do not implement those now except minimum deployment notes required to avoid unsafe production setup.

---

# 54. TESTING

Add targeted P4.1 tests where useful.

Examples:

* environment validation
* health endpoint
* cron authorization
* bootstrap guard
* production cookie config where practically testable

Do not add tests merely for documentation.

Run full integration suite against `his_test`.

---

# 55. PRODUCTION SMOKE TEST

Where practical locally or via production build:

1. build production app
2. start production server
3. health endpoint returns healthy
4. login works
5. one simple authenticated page opens
6. DB connection works
7. protected cron endpoint rejects invalid token
8. valid controlled invocation runs safely
9. shutdown cleanly

Do not require external cloud deployment during this batch unless one is already configured.

---

# 56. SECURITY

P4.1 must preserve:

* restricted runtime DB account
* secure cookies
* organization isolation
* branch isolation
* server-side RBAC
* protected scheduler endpoints
* secret isolation

Do not weaken any protection for deployment convenience.

---

# 57. KNOWN PRODUCT BACKLOG

Do NOT reopen:

* Radiology amendment
* Pharmacy catalog semantic cross-check
* Pharmacy partial/invoiced returns
* PO approval
* reconciliation module
* employee self-service
* leave overlap
* regulatory adapters
* inactive branch picker cleanup
* Provider/User edit-after-create
* notification improvements

unless directly required for deployment operation.

---

# 58. DELIVERABLE

Create:

`P4_1_PRODUCTION_ENVIRONMENT_DEPLOYMENT_ARCHITECTURE_REPORT.md`

Required sections:

## Executive Summary

## Current Deployment Architecture Traced

## Environment Variables

## Environment Validation

## Database Connection Architecture

## Runtime DB Role / Migration DB Role

## Hosting Architecture Evaluation

## Recommended Initial Production Architecture

## Recommended Scale-Up Architecture

## Serverless vs Long-Lived Decision

## Web / Worker / Scheduler Design

## Outbox Production Scheduling

## Cron / Worker Security

## Authentication / Cookie Production Configuration

## Email Adapter / Password Reset Deployment

## File / Object Storage Decision

## Health / Readiness Checks

## CI Pipeline

## Environment Separation

## Staging Strategy

## Production Seeding / Bootstrap Admin

## Connection Pooling

## Horizontal Scaling Readiness

## Deployment Procedure

## Files Changed

## Tests Added / Updated

## Production Smoke Test

## Security Review

## Remaining Deployment Backlog

## Regression Status

Include:

* Prisma validate
* migration status
* typecheck
* lint
* integration tests
* production build

Integration database:

Host:
Port:
Database:
Remote Supabase: NOT USED

Do not expose credentials.

---

# 59. STOP CONDITION

After P4.1:

STOP.

Do NOT begin P4.2.

Do NOT begin regulatory work.

Do NOT perform a whole-project audit.

Return the P4.1 report for review.

The goal is:

> Establish a production architecture and deployment foundation that can safely run the existing HIS for real clinics without compromising database security, authentication, event delivery, environment isolation, or operational maintainability.
